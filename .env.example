# .env.example
# Copy this file to .env and adjust values as needed.
# Both settings are optional — Fluxus falls back to sensible defaults
# if .env is missing entirely.

FLUXUS_STORE_ADDRESS=sqlite:///data/runtime.sqlite
LOG_DIR=logs